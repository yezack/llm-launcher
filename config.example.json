{
  "_comment": "主配置模板。复制为 config.json 后按需修改。顶层放公共设置；每个模型一个独立文件放 config.d/（改错一个不影响其他）。models 列表用 file 指向 config.d/ 下的文件。",
  "base_dir": "D:/path/to/models",
  "server_executable": "bin/llama-server.exe",
  "host": "0.0.0.0",
  "port": 8080,
  "use_guard": false,
  "env": {},
  "models": [
    { "file": "config.d/example-model.json" }
  ]
}
